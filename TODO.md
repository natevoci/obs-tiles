# TODO list

- Add an option to toggle showing or hiding the border (and margins) for tile groups. Defaults to true. Note, border still always shows in inline edit mode.
- Update auto-backup functionality to include the date (iso format) and machine name in the filename the backed up config file.
- Add the ability to re-order configs in the settings.
